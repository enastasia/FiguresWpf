<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <Color x:Key="Bg0">#FF0F111A</Color>
    <Color x:Key="Bg1">#FF151A28</Color>
    <Color x:Key="Bg2">#FF1B2236</Color>
    <Color x:Key="Stroke">#FF2D375A</Color>
    <Color x:Key="Text">#FFE6E9F2</Color>
    <Color x:Key="TextDim">#FFA8B0C5</Color>
    <Color x:Key="Accent">#FF7AA2F7</Color>

    <SolidColorBrush x:Key="BrushBg0" Color="{StaticResource Bg0}"/>
    <SolidColorBrush x:Key="BrushBg1" Color="{StaticResource Bg1}"/>
    <SolidColorBrush x:Key="BrushBg2" Color="{StaticResource Bg2}"/>
    <SolidColorBrush x:Key="BrushStroke" Color="{StaticResource Stroke}"/>
    <SolidColorBrush x:Key="BrushText" Color="{StaticResource Text}"/>
    <SolidColorBrush x:Key="BrushTextDim" Color="{StaticResource TextDim}"/>
    <SolidColorBrush x:Key="BrushAccent" Color="{StaticResource Accent}"/>

    <Style TargetType="Window">
        <Setter Property="Background" Value="{StaticResource BrushBg0}"/>
        <Setter Property="Foreground" Value="{StaticResource BrushText}"/>
        <Setter Property="FontFamily" Value="Segoe UI"/>
        <Setter Property="FontSize" Value="13"/>
    </Style>

    <Style TargetType="TextBox">
        <Setter Property="Background" Value="{StaticResource BrushBg1}"/>
        <Setter Property="Foreground" Value="{StaticResource BrushText}"/>
        <Setter Property="BorderBrush" Value="{StaticResource BrushStroke}"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="Padding" Value="10,6"/>
        <Setter Property="Margin" Value="0,6,0,10"/>
    </Style>

    <Style TargetType="Button">
        <Setter Property="Background" Value="{StaticResource BrushBg2}"/>
        <Setter Property="Foreground" Value="{StaticResource BrushText}"/>
        <Setter Property="BorderBrush" Value="{StaticResource BrushStroke}"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="Padding" Value="12,8"/>
        <Setter Property="Cursor" Value="Hand"/>
        <Setter Property="Margin" Value="0,6,0,0"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Border x:Name="bd" CornerRadius="8" Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}">
                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="bd" Property="BorderBrush" Value="{StaticResource BrushAccent}"/>
                        </Trigger>
                        <Trigger Property="IsPressed" Value="True">
                            <Setter TargetName="bd" Property="Opacity" Value="0.85"/>
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter TargetName="bd" Property="Opacity" Value="0.5"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style TargetType="GroupBox">
        <Setter Property="Margin" Value="0,8,0,0"/>
        <Setter Property="BorderBrush" Value="{StaticResource BrushStroke}"/>
        <Setter Property="Foreground" Value="{StaticResource BrushTextDim}"/>
    </Style>

</ResourceDictionary>
